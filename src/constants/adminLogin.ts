/** Seeded system administrator — same login as marketplace; use /admin after sign-in. */
export const SYSTEM_ADMIN_LOGIN = {
  email: 'admin9090@ad.com',
  password: 'admin123',
  displayName: 'Admin',
} as const;
